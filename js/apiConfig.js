/* ============================================================================
   apiConfig.js
   ----------------------------------------------------------------------------
   Endereço da sua API (o servidor Node.js publicado, que fala com o
   Postgres). É o ÚNICO ajuste necessário no front-end pra conectar tudo.

   Depois de publicar a API (Render, Railway, Fly.io etc.), troque a URL
   abaixo pela URL real, SEM barra "/" no final. Exemplo:

     window.API_BASE_URL = 'https://brincar-de-desejo-api.onrender.com';

   Enquanto estiver testando localmente (rodando "npm run dev" dentro da
   pasta server/), pode deixar assim:

     window.API_BASE_URL = 'http://localhost:3000';
   ============================================================================ */
window.API_BASE_URL = 'https://SUA-API-AQUI.onrender.com';
